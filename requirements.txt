# Python dependencies for the PhysaRoute reproduction pipeline.
# Tested with Python 3.10 – 3.12.
#
# Install with:
#     python3 -m pip install -r requirements.txt
#
# On PEP-668-marked systems (Debian 12+, Ubuntu 24.04+) add
# `--break-system-packages` or, preferably, use a virtual environment.

numpy>=1.24
scipy>=1.10
matplotlib>=3.7
pandas>=2.0
pyyaml>=6.0
